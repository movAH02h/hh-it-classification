# HH IT-Classification

Пайплайн классификации грейдов (Junior/Middle/Senior) для IT-вакансий.

## Запуск
1. Поместите `hh_cleaned.csv` в папку `data/`.
2. Установите зависимости: `pip install -r requirements.txt`.
3. Запустите: `python src/main.py`.
